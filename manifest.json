{
  "manifest_version": 3,
  "name": "Design Extractor",
  "version": "0.1.0",
  "description": "Clica num elemento e copia o design dele (HTML + estilos computados) pra colar em qualquer agente de IA.",
  "permissions": ["activeTab", "scripting"],
  "background": { "service_worker": "background.js" },
  "action": { "default_title": "Extrair design (clique, depois clique no elemento)" }
}
